# Mesh-wide: PERMISSIVE baseline (this is for using certificates as a default strategy, not enforcement)
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio-system
spec:
  mtls:
    mode: PERMISSIVE