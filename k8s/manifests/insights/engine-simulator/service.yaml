apiVersion: v1
kind: Service
metadata:
  name: insights-engine-sim-service
  namespace: insights-engine-sim
spec:
  selector:
    app: insights-engine-sim
    version: v1
  ports:
  - name: http
    port: 8080
    targetPort: 8000