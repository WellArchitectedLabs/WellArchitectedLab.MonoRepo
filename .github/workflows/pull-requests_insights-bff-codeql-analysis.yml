# .github/workflows/codeql-insights-bff.yml
name: CodeQL Analysis - Insights Bff

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'src/insights/bff/**'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'src/insights/bff/**'
  workflow_dispatch:

jobs:
  call-codeql-template:
    uses: ./.github/workflows/template_csharp-codeql.yml
    with:
      solution: 'src/insights/bff/WeatherForecast.sln'
