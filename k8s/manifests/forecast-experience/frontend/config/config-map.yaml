apiVersion: v1
kind: ConfigMap
metadata:
  name: wf-exp-frontend-configmap
  namespace: frontend
  labels:
    app: wf-exp-frontend-configmap
data:
  appSettings.yaml: |
    # yaml file
    backend_url: http://api.weatherly.local
