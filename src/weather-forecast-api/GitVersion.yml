mode: ContinuousDelivery

branches:
  main:
    regex: ^main$
    increment: Patch
  develop:
    regex: ^develop$
    increment: Minor
  feature:
    regex: ^feature[/-]
    increment: Inherit

ignore:
  sha: []