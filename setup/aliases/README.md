Some git aliases are developed as part of this repo.
The most important one is git pr.
Please use a powershell runner for installing the alias.
Available runners are available even for non windows environments. Typically for Mac, the powershell runner is installed easily via home brew. 