apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana
spec:
  source:
    helm:
      valuesObject:
        env:
          GF_DATABASE_HOST: "$(ENV)-grafana-database-postgresql.monitoring.svc.cluster.local:5432"
