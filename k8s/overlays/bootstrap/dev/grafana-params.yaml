apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana
spec:
  source:
    helm:
      parameters:
      - name: environmentPrefix
        value: dev
      - name: environment  
        value: dev