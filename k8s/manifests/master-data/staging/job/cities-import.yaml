apiVersion: batch/v1
kind: Job
metadata:
  name: cities-import
  namespace: backend
  labels:
    app: cities-import
spec:
  template:
    metadata:
      name: cities-import
      labels:
        app: cities-import
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      securityContext:
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 0
      tolerations:
      - key: workload
        operator: 'Equal'
        value: 'weather-forecast'
        effect: 'NoSchedule'
      nodeSelector:
        kubernetes.azure.com/mode: user
      initContainers:
      - name: postgres-sanity-guard
        image: acrdevtshpwn.azurecr.io/postgres-sanity-guard:1.0.0
        env:
          - name: DB_HOST
            value: master-data-database-postgresql.backend.svc.cluster.local
          - name: DB_PORT
            value: "5432"
          - name: DB_NAME
            value: master_data
          - name: DB_APP_USER
            valueFrom:
              secretKeyRef:
                name: master-data-db-secrets
                key: DB_APP_USER
          - name: DB_APP_PASSWORD
            valueFrom:
              secretKeyRef:
                name: master-data-db-secrets
                key: DB_APP_PASSWORD
          - name: POSTGRES_SUPERUSER_PASSWORD
            valueFrom:
              secretKeyRef:
                name: master-data-db-secrets
                key: POSTGRES_SUPERUSER_PASSWORD
          # Optional: customize retry behavior
          - name: MAX_RETRIES
            value: "30"
          - name: RETRY_INTERVAL
            value: "2"
      containers:
      - name: cities-import
        image: acrdevtshpwn.azurecr.io/cities-initializer:0.1.0-PullRequest0089.460-8063c85
        args: ["--input", "/mnt/blob/capitals.csv"]
        env:
          - name: POSTGRES_DSN
            valueFrom:
              secretKeyRef:
                name: master-data-db-secrets
                key: DB_APP_DSN
        volumeMounts:
        - name: imports-blob
          mountPath: "/mnt/blob"
      
      volumes:
      - name: imports-blob
        persistentVolumeClaim:
          claimName: dev-pvc-az-blob-imports2
      
      restartPolicy: Never