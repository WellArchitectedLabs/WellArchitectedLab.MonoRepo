# Default deny all ingress and egress in engine-simulator
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: insights-engine-sim
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress