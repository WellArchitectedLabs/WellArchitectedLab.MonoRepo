<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Target -->
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>

    <!-- NuGet identity -->
    <PackageId>InsightsCollector.Client</PackageId>
    <Version>$(Version)</Version>

    <!-- Ownership -->
    <Authors>Mohamed Baligh H.</Authors>
    <Company>devpragmax</Company>

    <!-- Presentation -->
    <Title>Insights Collector Client SDK</Title>
    <Description>
      Official .NET client SDK for Insights Collector API.
      Provides strongly-typed APIs, client registration helpers,
      and fluent first-class integration with Insights Collector services.
    </Description>
    <Summary>.NET client library for Insights Collector APIs</Summary>
    <PackageTags>insights-collector sdk client api dotnet</PackageTags>

    <!-- Links -->
    <PackageProjectUrl>
      https://github.com/WellArchitectedLabs/WellArchitectedLab.MonoRepo
    </PackageProjectUrl>

    <RepositoryUrl>
      https://github.com/WellArchitectedLabs/WellArchitectedLab.MonoRepo
    </RepositoryUrl>
    <RepositoryType>git</RepositoryType>

    <!-- Legal -->
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <RequireLicenseAcceptance>false</RequireLicenseAcceptance>

    <!-- NuGet UI assets -->
    <PackageReadmeFile>README.md</PackageReadmeFile>

    <!-- Release notes -->
    <PackageReleaseNotes>
      See https://github.com/WellArchitectedLabs/WellArchitectedLab.MonoRepo/releases
    </PackageReleaseNotes>

    <!-- Packaging -->
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
  </PropertyGroup>

  <ItemGroup>
    <!-- NuGet assets -->
    <None Include="README.md" Pack="true" PackagePath="/" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.0" />
    <PackageReference Include="Refit" Version="10.0.1" />
    <PackageReference Include="Refit.HttpClientFactory" Version="10.0.1" />
  </ItemGroup>
</Project>
