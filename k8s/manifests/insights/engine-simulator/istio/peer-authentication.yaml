# Per mesh namespace — copy this for each enrolled namespace
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: insights-engine-sim      # change per namespace
spec:
  mtls:
    mode: STRICT