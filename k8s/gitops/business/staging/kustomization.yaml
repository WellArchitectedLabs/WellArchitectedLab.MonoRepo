apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  namespace: argocd
  name: bootstrap-kustomization

# reference all manifests from base folder
resources:
  - portgres.yaml
  - sealed-secrets.yaml